[project]
name = "datacommons-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = []
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.10.1",
]

[project.scripts]
datacommons-mcp = "datacommons.mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/datacommons"]
